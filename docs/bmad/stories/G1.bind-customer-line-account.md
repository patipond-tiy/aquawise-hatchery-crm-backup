# Story G1: Bind a Customer's LINE Account to Their CRM Record

Status: ready-for-dev

## Story

As a Rep,
I want to mint a one-shot bind link for a customer and send it to them via LINE,
so that their LINE user_id gets connected to their farm record and future Flex messages can reach them.

> **Scope clarification (LINE bot Epic K integration).** G1 binds a **nursery customer** (`customers.line_id`) — a B2B `โรงอนุบาล` operator that the hatchery sells PL to. It is the channel used by stories D2 (quote send), C4 (cert send), E4 (alert notify), and the cron broadcasts in D3/G4. It is NOT the path used by individual **farmers** (พี่ปลา) who claim a batch via LIFF wizard. Farmer claims are written to `batch_claims` by the LINE bot via Stories K2/K3 — there is no CRM-side bind link, no `customers` row, and no `line_id` on `customers` for farmers. The two identity systems coexist deliberately: a nursery is a commercial counterparty; a farmer is an anonymous downstream consumer whose only relationship with the CRM is "I scanned this batch code". Do not conflate them in code or queries.

## Acceptance Criteria

1. On the customer card, a "Connect LINE" CTA is visible when `customer.line_id IS NULL`
2. Clicking the CTA calls a server action that mints a `customer_bind_tokens` row (26-char ULID, 7-day expiry) and returns a LIFF link `liff.line.me/{liffId}/bind?token=...`
3. The rep copies the link and sends it to the farmer via their current channel
4. Farmer opens the link in LINE — the LIFF bind page (in the bot worker repo) authenticates with LINE Login, POSTs to CRM `/api/line/bind` with `{token, lineUserId, lineProfile}`, server consumes the token, sets `customers.line_id`, upserts `line_users`, creates a `chat_threads` row, and deletes the token (one-shot)
5. Token reuse after consumption returns a 409 error; expired tokens (>7 days) return a 410
6. Customer card updates to show a "LINE connected" state and last-seen timestamp

## Tasks / Subtasks

- [ ] Task 1 — CRM-side: Create the bind API route (AC: #2, #4, #5)
  - [ ] Create `app/api/line/bind/route.ts` with two handlers:
    - `POST /api/line/bind` — mint token: server action callable from customer card; inserts `customer_bind_tokens(id ULID, customer_id, hatchery_id, expires_at=now()+7d, consumed_at=null)`; returns LIFF URL `liff.line.me/${LIFF_ID}/bind?token=${ulid}`
    - `POST /api/line/bind` (body: `{token, lineUserId, lineProfile}`) — consume token: verify not expired, not consumed; set `customers.line_id`; upsert `line_users`; insert `chat_threads` row; set `consumed_at`; return `{ok: true, customerId}`. **Must be POST, not GET** — this is a mutating operation.
  - [ ] Confirm `LIFF_ID` env var is documented in `.env.example` with a note that it lives in the bot worker; CRM only needs the ID to construct the URL (no LINE SDK import in CRM)
  - [ ] Use service-role Supabase client in the consume handler (RLS bypassed for cross-service write from bot worker)
- [ ] Task 2 — CRM-side: Wire "Connect LINE" CTA on customer card (AC: #1, #2, #3)
  - [ ] Add conditional CTA to customer card: render only when `customer.line_id == null`
  - [ ] Server action `mintBindLink(customerId)` calls `POST /api/line/bind` and returns the LIFF URL; copy-to-clipboard on success + toast "ลิงก์พร้อมแล้ว — คัดลอกแล้ว"
  - [ ] After successful bind (polled or optimistic), card switches to "LINE connected" chip; last-seen renders from `line_users.last_seen_at`
- [ ] Task 3 — CRM-side: Mock-mode support (AC: #1, #2)
  - [ ] `lib/mock/api.ts` — add `mintBindLink()` that returns a fake LIFF URL; mock customer with `line_id=null` to exercise the CTA; mock customer with `line_id='U...'` to exercise the connected state
- [ ] Task 4 — Bot worker repo (separate): LIFF bind page — out of scope for this CRM story
  - [ ] Note: `/liff/bind` page in `aquawise-line-bot` repo does LINE Login, calls CRM `POST /api/line/bind` with `{token, lineUserId, lineProfile}`, handles 409/410 error states with user-friendly Thai copy
  - [ ] Coordinate: LIFF ID must be provisioned and shared with CRM team before `.v` can pass
- [ ] Task 5 — Tests (AC: #5)
  - [ ] `tests/line/bind.test.ts`:
    - Token expires at exactly 7 days; request at 7d+1s returns 410
    - Reuse of already-consumed token returns 409
    - Valid consumption sets `customers.line_id` and `consumed_at`
    - RLS: `counter_staff` role can call the mint action; service-role consumer can bypass RLS on write
- [ ] Task 6 — Live verification (AC: #1–#6)
  - [ ] Set `USE_MOCK=false`; confirm `customer_bind_tokens` table exists (migration 006)
  - [ ] Mint a link as a counter_staff user; verify row in `customer_bind_tokens` with correct expiry
  - [ ] Open link in LINE (LIFF page must be deployed in bot worker repo); confirm `customers.line_id` is populated; confirm `customer_bind_tokens` row has `consumed_at` set
  - [ ] Attempt to reuse the link; confirm 409 response

## Dev Notes

### Architecture Constraints

- All API calls via `lib/api/index.ts` facade — never Supabase directly from pages
- `app/api/line/bind/route.ts` is a Next.js API Route (not a Server Action) because the consume path is called by the bot worker service over HTTP, not by a browser form
- The mint path (`POST`) is wrapped as a server action callable from the customer card component
- `'use client'` opt-in only; server components by default
- No `any` in app code; `as` casts allowed only in adapters
- CRM never imports `@line/bot-sdk` or calls LINE Messaging API directly

### Cross-Service Boundary

**CRM-side (this repo):**
| File | Role |
|------|------|
| `app/api/line/bind/route.ts` | POST: mint token; POST (with body `{token, lineUserId, lineProfile}`): consume token (called by bot worker) — **both paths are POST, not GET** (consuming is a mutating operation) |
| `app/[locale]/(dashboard)/customers/actions.ts` | Server action `mintBindLink(customerId)` |
| `components/` (customer card) | "Connect LINE" CTA, conditional on `line_id == null` |
| `lib/mock/api.ts` | Mock `mintBindLink()` returning fake LIFF URL |
| `tests/line/bind.test.ts` | Token expiry, reuse, consume coverage |

**Bot worker repo (separate — `aquawise-line-bot`, Cloud Run `asia-southeast1`):**
- `/liff/bind` LIFF page: LINE Login → POST to CRM `/api/line/bind` → success/error UI
- `LIFF_ID`, `LINE_CHANNEL_ACCESS_TOKEN`, `LINE_CHANNEL_SECRET` env vars live here — not in CRM

### Files to Touch (CRM-side only)

| File | Action |
|------|--------|
| `app/api/line/bind/route.ts` | CREATE — does not exist (only `/api/webhooks/stripe/` exists today) |
| `app/[locale]/(dashboard)/customers/actions.ts` | ADD `mintBindLink()` server action |
| Customer card component | ADD conditional "Connect LINE" CTA |
| `lib/mock/api.ts` | ADD mock bind token support |
| `.env.example` | ADD `LIFF_ID` with comment: "LIFF app ID from bot worker team; CRM uses this to construct the bind URL only" |
| `tests/line/bind.test.ts` | CREATE |

### Schema

`customer_bind_tokens` already exists in `supabase/migrations/006_line_integration.sql`. No new migration needed. Confirm the column shape before coding:
- `id` — 26-char ULID (not UUID)
- `customer_id` — FK to `customers`
- `hatchery_id` — FK to `hatcheries` (for RLS scoping)
- `expires_at` — `timestamptz`, set to `now() + interval '7 days'`
- `consumed_at` — `timestamptz`, nullable; set on consumption

### RBAC

- `counter_staff` can mint bind links (`customer:write` action)
- Auditor cannot mint (`customer:write` not in auditor permission set)
- Consume path uses service-role (called by bot worker, not a browser session); RLS bypassed intentionally
- Use `can(role, 'customer:write')` from `lib/rbac.ts` before calling the mint action

### RLS Tables

| Table | Operation | Who | Migration |
|-------|-----------|-----|-----------|
| `customer_bind_tokens` | INSERT (mint) | `hatchery_members` with `customer:write` | `006_line_integration.sql` |
| `customer_bind_tokens` | SELECT + UPDATE `consumed_at` (consume) | service-role | `006_line_integration.sql` |
| `customers` | UPDATE `line_id` | service-role (consume path) | `001_init.sql` |
| `line_users` | UPSERT | service-role | `006_line_integration.sql` |
| `chat_threads` | INSERT | service-role | `006_line_integration.sql` |

### i18n Keys

Add to both `messages/th.json` and `messages/en.json` under `"line"` namespace:

```jsonc
// messages/th.json
"line": {
  "connect_cta": "เชื่อม LINE",
  "connected_chip": "LINE เชื่อมแล้ว",
  "link_copied_toast": "ลิงก์พร้อมแล้ว — คัดลอกแล้ว",
  "bind_error": "ไม่สามารถสร้างลิงก์ได้ กรุณาลองใหม่"
}

// messages/en.json
"line": {
  "connect_cta": "Connect LINE",
  "connected_chip": "LINE connected",
  "link_copied_toast": "Link ready — copied",
  "bind_error": "Could not generate link. Please try again."
}
```

### Sequencing Constraint

G1 must land **before** D2, C4, E4, F3, and G2. Those stories all require a bound `line_id` on the customer to push Flex messages. Without G1, the queue can enqueue events but the bot worker has no `line_user_id` to deliver to.

### Testing

```bash
# Run bind unit tests
pnpm vitest run tests/line/bind.test.ts

# Typecheck and lint (must be clean before marking .v done)
pnpm typecheck
pnpm lint
```

### References

- [Source: docs/product-spec/03-user-stories.md#G1]
- [Source: docs/work-breakdown/MATRIX.md#G1]
- [Source: docs/product-spec/05-line-integration.md — Week 1: bind the loop]
- [Source: docs/bmad/stories/_integration-with-line-bot-Epic-K.md — identity-boundary note]
- [Source: docs/bmad/stories/K3.batch-claim-api.md — farmer-side path; explicitly NOT G1's flow]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
