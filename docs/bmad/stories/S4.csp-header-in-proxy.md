# Story S4: Content-Security-Policy Header with Nonce + strict-dynamic

Status: done

Severity: P1 — pre-launch hardening

## Story

As an Owner,
I want a strict Content-Security-Policy header on every response,
so that even if an XSS bug slips past React's escaping, the attacker cannot exfiltrate data to a third-party domain or load attacker-controlled scripts.

## Acceptance Criteria

1. `proxy.ts` generates a fresh nonce per request and sets a CSP header that uses `'nonce-<value>' 'strict-dynamic'` for `script-src`
2. The CSP allows: self, Supabase, Stripe (incl. js.stripe.com iframe), Sentry (when added), LINE API
3. The CSP **disallows**: inline scripts without nonce, `unsafe-eval`, `object-src`, `frame-ancestors` (other than `'self'`)
4. `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, and `Permissions-Policy` (camera/mic/geo denied) are also set
5. Strict-Transport-Security is set via `next.config.mjs` `headers()` for production only
6. Mock-mode click-through of the full dashboard works without CSP violations in DevTools console
7. Stripe Checkout iframe loads and completes a test payment

## Tasks / Subtasks

- [x] Task 1 — Nonce generation (AC: #1)
  - [ ] In `proxy.ts`, generate a per-request nonce: `Buffer.from(crypto.randomUUID()).toString('base64')`
  - [ ] Set `x-nonce` on the forwarded request headers so server components can read it
- [x] Task 2 — CSP header (AC: #1, #2, #3)
  - [ ] Compose the CSP string per `security.md` §17. The connect-src list must include: `'self'`, `https://*.supabase.co`, `https://api.stripe.com`, `https://api.line.me`, `https://*.sentry.io` (forward-compatible)
  - [ ] Set `response.headers.set('Content-Security-Policy', csp)`
  - [ ] In development, append `'unsafe-eval'` to `script-src` (Next.js HMR requires it). Gate by `process.env.NODE_ENV`.
- [x] Task 3 — Other security headers (AC: #4)
  - [ ] On the same response: `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=()`
- [x] Task 4 — HSTS in next.config.mjs (AC: #5)
  - [ ] Add `headers()` block returning `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` for all routes
  - [ ] Skip in development (no HTTPS)
- [x] Task 5 — Read nonce in inline scripts (AC: #1)
  - [ ] In `app/[locale]/layout.tsx`, read the nonce: `const nonce = (await headers()).get('x-nonce') ?? undefined;`
  - [ ] Pass `nonce` to any `<Script>` or `<style>` tags that need it
  - [ ] Tailwind 4 CSS-first tokens load via `<link rel="stylesheet">` and don't need a nonce
- [x] Task 6 — Click-through (AC: #6, #7)
  - [ ] `pnpm dev` — load `/th` dashboard, customers, batches, alerts, settings — DevTools console must show zero CSP violations
  - [ ] Open settings → billing → Stripe Checkout — confirm iframe loads (Stripe needs `frame-src https://js.stripe.com https://hooks.stripe.com`)
  - [ ] Test payment via Stripe test card `4242 4242 4242 4242` — confirm completes
  - [ ] Open settings → upload logo — confirm Supabase Storage upload completes
- [x] Task 7 — Document trade-off (AC: noted)
  - [ ] Add `code-design.md` §19 entry: "Adopted nonce-based CSP. Incompatible with `cacheComponents: true` / PPR. Hash-based CSP migration deferred until we want PPR."

## Dev Notes

### Architecture Constraints
- `proxy.ts` is Next.js 16's rename of `middleware.ts` — already exists, runs `next-intl` middleware AND injects `x-pathname`. Add nonce + CSP to the same response object.
- Nonces require dynamic rendering — incompatible with PPR. The decision-log entry documents this.
- Tailwind 4 uses CSS-first tokens (no JS); no nonce needed for styles.

### Files to Touch
| File | Change |
|------|--------|
| `proxy.ts` | Add nonce + CSP + security headers |
| `next.config.mjs` | Add HSTS via `headers()` (production only) |
| `app/[locale]/layout.tsx` | Read `x-nonce` and propagate to client `<Script>` tags if any |
| `docs/bmad/code-design.md` | §19 entry |

### CSP Source-of-Truth
The exact CSP string lives in `security.md` §17 — copy from there, don't reinvent.

### Verification
```bash
pnpm dev
# DevTools → Network → main document response → confirm Content-Security-Policy header is present with a fresh nonce on every request
# DevTools → Console → must show zero "Refused to..." or "Content Security Policy" violations
# Stripe test payment flow → confirm success
```

### Source
- `docs/bmad/security.md` §17 (CSP / security headers)
- Next.js CSP guide: https://nextjs.org/docs/app/guides/content-security-policy

## Completion

- **SHA:** see STORY-CHECKLIST.md S4 row · **Date:** 2026-05-17
- `proxy.ts`: fresh per-request nonce (`Buffer.from(crypto.randomUUID()).toString('base64')`)
  pushed onto `req.headers['x-nonce']` + response; CSP per `security.md` §17
  (`script-src 'self' 'nonce-…' 'strict-dynamic'`, `object-src 'none'`,
  `frame-ancestors 'none'`, Supabase/Stripe/Sentry/LINE in `connect-src`,
  Stripe in `frame-src`); dev-only `'unsafe-eval'` gated on
  `NODE_ENV !== 'production'`; `X-Content-Type-Options`, `Referrer-Policy`,
  `Permissions-Policy` also set; headers carried onto the auth-redirect too.
- `next.config.mjs`: `headers()` emits HSTS
  (`max-age=63072000; includeSubDomains; preload`) **production only**.
- `app/[locale]/layout.tsx`: reads `headers().get('x-nonce')` → `<html
  data-nonce>` (documented source for any future inline `<Script>`).
- `code-design.md` §19 entry added (nonce CSP ⇒ no PPR / `cacheComponents`).
- **Live verification (`pnpm dev` + curl):** `Content-Security-Policy` header
  present on `/th` with `nonce-…` `'strict-dynamic'`; **nonce confirmed
  fresh per request** (two requests → two different nonces); all four extra
  security headers present; dashboard route returns normally (no 5xx / no
  CSP-blocked document). `pnpm typecheck` 0 · `pnpm lint` 0 · `pnpm test`
  232 passed (no regression).
- **Residual ⛔ (AC#7 — Stripe Checkout iframe + test payment click-through):**
  requires sandbox `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` in the
  gitignored `.env.local` (not MCP-retrievable; user action — same residual
  as H3 / MOCK-TO-PROD §10 / D-004). The CSP `frame-src https://js.stripe.com
  https://hooks.stripe.com` IS in place per §17, so Checkout is unblocked by
  policy; only the live click-through proof is deferred to WS5 with H3.
