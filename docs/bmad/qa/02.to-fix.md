# Human-Review Fix List

> Extracted from `01.qa-user-story-audit.md` (FIX-REVIEW rows only).
> These need product judgment before a coding agent can act on them.
> Generated 2026-05-13.

## Count by epic

| Epic | FIX-REVIEW |
|------|-----------|
| A    | 0 |
| B    | 0 |
| **C**| **1** |
| **D**| **2** |
| E    | 0 |
| G    | 0 |
| H    | 0 |
| X    | 0 |
| **Total** | **3** |

The other 26 stories are 6 KEEP + 20 FIX-AUTO — see
`01.qa-user-story-audit.md` for those.

---

## Epic C

### C2 — Browse and review batches

- **AC ref:** AC5 (auditor SECURITY INVOKER view, migration 013) — also persona drift, story header changed from "As an Auditor" (legacy `03-user-stories.md#C2`) to "As a Manager (P2)".
- **Decision needed:** does the auditor SECURITY INVOKER view (migration 013) ship in H1, or defer to H3 alongside the auditor persona?
- **Open question:** is there a confirmed hatchery customer asking for auditor-scoped batch browsing in H1, or is this H3 infra leaking forward? Rule#7 ("slow is right") points to defer; but landing the schema early is cheap if no H1 user consumes it.
- **Why REVIEW not AUTO:** `01-personas.md` P5 lists `auditor` as "planned, Phase H3" — building an `auditor`-only DB view in H1 is a phase-boundary call only a human can ratify.

---

## Epic D

### D2 — Send a quote in one-tap

- **AC ref:** AC#4 (quote Flex "ตอบรับ" button opens a LIFF chat thread).
- **Decision needed:** strip the "ตอบรับ" → LIFF chat thread CTA from AC#4 for H1, or defer the whole story to H3?
- **Open question:** what should farmers tap on a quote Flex in H1? Options:
  - (a) point CTA at the G3p temporary LIFF placeholder (already specced in G3p AC5)
  - (b) plain LINE-OA URL with no AquaWise surface
  - (c) drop the reply CTA entirely (Flex shows quote details only, no reply path)

  Each option has different rule#4 implications (we don't replace LINE chit-chat) and different brand-voice cost.
- **Why REVIEW not AUTO:** `00-overview.md` is explicit that two-way chat is H3; AC#4 promises a capability the product has deliberately chosen not to ship until H3. Picking the H1 substitute is a brand-voice + UX call. Also missing ⚠ flag for the v0.5 hatchery customer-relationship hypothesis (`06` §3 Job 3).

### D3 — Broadcast to a restock cohort

- **AC ref:** AC#3 + Task 1 (Dev Notes RBAC uses `can(role, 'settings:write')` for broadcast; migration 006 RLS grants `line_outbound_events` INSERT to `owner + counter_staff`).
- **Decision needed:**
  1. Can `counter_staff` send broadcasts (matching migration 006 RLS), or is broadcast owner-only (matching the story's Dev Notes intent)?
  2. Which RBAC action covers broadcast — keep `settings:write`, or add a dedicated `broadcast:write` to `lib/rbac.ts`?
- **Open question:** do hatchery owners want counter staff fanning out to all "restock-this-week" farms autonomously, or is broadcast a deliberate owner-only signal? The product answer drives both the RBAC action label and the RLS policy on `line_outbound_events`.
- **Why REVIEW not AUTO:** the migration 006 RLS and the Dev Notes RBAC disagree on who can broadcast; reconciling them requires deciding *who broadcasts*, not just renaming an action. Also missing H2 phase tag in header + missing ⚠ flag on the customer-base hypothesis (both FIX-AUTO once the broadcast policy is settled).

---

## After these are resolved

- The 20 FIX-AUTO items in `01.qa-user-story-audit.md` are safe to batch into a single coding pass. Several touch the same story files as the FIX-REVIEW items above (e.g., D2's ⚠ flag, D3's phase tag + ⚠ flag), so it's cheaper to resolve C2/D2/D3 first and fold the auto-fixes into the same edits.
- 6 KEEP items (B1, B2, D1, E1, E3, H1) need no action this round.
