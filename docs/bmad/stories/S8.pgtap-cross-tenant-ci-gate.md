# Story S8: pgTAP Cross-Tenant Test as CI Required Check

Status: ready-for-dev

Severity: P0 — required for launch (cross-tenant data leaks are the #1 multi-tenant SaaS incident class)

## Story

As an Owner,
I want every pull request to run an automated test that proves a user from tenant A cannot read or write any row belonging to tenant B,
so that a developer cannot accidentally ship an RLS regression — the most catastrophic class of bug in a multi-tenant SaaS.

## Acceptance Criteria

1. `supabase/tests/cross-tenant.sql` exists and contains pgTAP assertions for every tenant-scoped table in `architecture.md` §5
2. The test creates two Supabase users, gives them separate hatcheries, has user A insert one row per table, and asserts user B sees `count = 0` on every table
3. The test runs locally via `supabase test db`
4. CI workflow `.github/workflows/ci.yml` runs `supabase test db` as a required check; the job fails the PR if any assertion fails
5. Adding a new tenant-scoped table is required to update `cross-tenant.sql` in the same PR (lint guidance in `code-design.md` §8 step 8 + checklist update in §16)
6. A deliberately-broken RLS policy (e.g., `using (true)` on one table) causes the CI check to fail — verified by a one-commit branch that demonstrates the failure mode

## Tasks / Subtasks

- [ ] Task 1 — Inventory tenant-scoped tables (AC: #1)
  - [ ] Read `architecture.md` §5; list every row marked "All roles, own hatchery" or "Own hatchery's members"
  - [ ] Cross-check against `supabase/migrations/*.sql` to confirm every such table has RLS enabled
  - [ ] Current list (verify): `hatcheries`, `hatchery_members`, `customers`, `customer_cycles` (via customer FK), `batches`, `batch_buyers` (via batch FK), `pcr_results`, `alerts`, `notification_settings`, `scorecard_settings`, `customer_line_ids`, `line_outbound_events`, `team_invites`, `hatchery_brand`, `subscription_events`
- [ ] Task 2 — Author the pgTAP test (AC: #1, #2, #3)
  - [ ] Create `supabase/tests/cross-tenant.sql`
  - [ ] Use the `supabase-test-helpers` library: `tests.create_supabase_user('owner_a')`, `tests.create_supabase_user('owner_b')`
  - [ ] Set up two hatcheries; insert membership for owner_a and owner_b separately
  - [ ] For each table from Task 1:
    - As owner_a, insert one row with `hatchery_id = hatchery_a`
    - As owner_b, run `SELECT count(*) FROM <table> WHERE hatchery_id = <hatchery_a>`; assert `is(count, 0)`
    - For tables joined via FK (e.g., `batch_buyers` via `batches`), assert the cross-tenant read is still blocked
  - [ ] `supabase test db` — must pass locally
- [ ] Task 3 — CI workflow (AC: #4)
  - [ ] Add to `.github/workflows/ci.yml` a job `pgtap-cross-tenant` that:
    - Spins up Supabase local via the `supabase/setup-cli@v1` action
    - Runs `supabase db reset` to apply all migrations
    - Runs `supabase test db`
    - Reports failure to GitHub status
  - [ ] Mark as required in branch protection (manual GitHub UI step)
- [ ] Task 4 — Update documentation (AC: #5)
  - [ ] In `code-design.md` §8 step 8: explicitly require updating `cross-tenant.sql` when adding a new tenant-scoped table
  - [ ] In `code-design.md` §16 Data section: add a checkbox "If a new table: cross-tenant.sql is updated with that table"
  - [ ] In `security.md` §22: update the canonical test path
- [ ] Task 5 — Verify the gate fires (AC: #6)
  - [ ] On a throwaway branch, modify one policy to `using (true)` for, say, `customers`
  - [ ] Push the branch; confirm CI job `pgtap-cross-tenant` fails
  - [ ] Discard the branch
  - [ ] Paste the failed-run URL into this story's "Verification log" section

## Dev Notes

### Architecture Constraints
- pgTAP is the canonical Supabase test tool — see `code-design.md` §11
- Use transaction-wrapped tests (`BEGIN; …; ROLLBACK;`) so multiple test files don't pollute each other
- The test runs against a freshly-seeded local Supabase, not the production project — never run `supabase test db` against production

### Files to Touch
| File | Change |
|------|--------|
| `supabase/tests/cross-tenant.sql` | NEW |
| `.github/workflows/ci.yml` | Add `pgtap-cross-tenant` job |
| `docs/bmad/code-design.md` | §8 step 8 + §16 checklist update |
| `docs/bmad/security.md` | §22 cross-reference update |

### Dependencies
- Depends on `supabase-test-helpers` being available in the project. If not installed, add per: https://github.com/usebasejump/supabase-test-helpers

### RBAC / RLS / i18n
- No RBAC or i18n changes. RLS is what's being tested.

### Sources
- `docs/bmad/code-design.md` §11 (Testing strategy — pgTAP)
- `docs/bmad/security.md` §21 (RLS multi-tenant correctness) and §22 (Cross-tenant automated testing)
- Supabase Testing Overview: https://supabase.com/docs/guides/local-development/testing/overview
- pgTAP: https://supabase.com/docs/guides/database/extensions/pgtap
- §19 entry: 2026-05-15 · Cross-tenant pgTAP test — CI required check

### Verification log (fill in during Task 5)
- Date: ____
- Failed-run URL: ____
- Confirmed gate behavior: yes/no
