# Story K2: Versioned Batch Read API (`GET /api/v1/batches/:code`)

Status: done

> **Contract:** conforms to `docs/aquawise-updated-docs/K-INTEGRATION-CONTRACT.md`. JWT model = §3 (read-side `iss=line-bot, aud=nursery-crm`; token-refresh per §3); response schema + status ladder = §5; `species` field = §8/K6. Where this story and the contract disagree, the contract wins.

## Story

As **the LINE bot** (acting on behalf of a shrimp farmer claiming a Batch Code),
I want a stable, signed-JWT-authenticated CRM endpoint that returns the full batch payload by `batch_code`,
so that the farmer's pond setup can validate the code and bootstrap PCR + cohort + warning flows (LINE bot Stories K.1, K.7).

## Acceptance Criteria

1. **Route shape:** `GET /api/v1/batches/:code` lives at `app/api/v1/batches/[code]/route.ts`.
2. **Auth:** Every request requires `Authorization: Bearer <ES256-signed JWT>`. The token must carry `iss=line-bot`, `aud=nursery-crm`, and `exp` ≤ 15 minutes in the future. Public key for verification is loaded from `CRM_JWT_PUBLIC_KEY` env var (PEM). Invalid signature → 401. Missing token → 401. Expired token → 401 with `WWW-Authenticate: Bearer error="token_expired"`.
3. **Path validation:** `:code` must match `^B-[A-CDEFGHJKMNPQRSTUVWXYZ23456789]{6}$`. Otherwise 400 with `{ error: 'invalid_code_format' }`. (Note the alphabet excludes `0`, `O`, `1`, `I`, `l` to match `mint_batch_code()` in Story K1.)
4. **Happy-path response (200):** body matches the LINE bot's expectation (ADR-018):
   ```jsonc
   {
     "batch_code": "B-A4F2K7",
     "nursery_id": "h_01HQ...",
     "nursery_name": "ฮอตเชอรี่สมุทรสาคร",
     "nursery_contact": { "line_oa_id": "@aqx", "phone": "0812345678" } | null,
     "pl_stage": "PL12",
     "source_strain": "Charoen Pokphand SPF",
     "spawn_date": "2026-05-01",
     "pcr_results": { "wssv": "negative", "ehp": "negative", "ihhnv": "negative", "tsv": "negative" },
     "pcr_certificate_url": "https://<signed-url>" | null,
     "valid_until": "2026-06-12T00:00:00+07:00",
     "claimed_by_other": false
   }
   ```
   Fields sourced from `batches`, `pcr_results`, `batch_certs`, and the frozen `nursery_contact_snapshot` (Story K1).
5. **404 if `batch_code` unknown.** Returns `{ error: 'batch_not_found' }`.
6. **404 if batch exists but `published_at IS NULL`.** Same error body. We do not leak whether a code exists in draft state — same 404 either way.
7. **410 if `valid_until <= now()`.** Returns `{ error: 'batch_expired', expired_at: <iso> }`.
8. **409 if the calling LINE user has already claimed a different code AND this code is already claimed by another LINE user.** Body: `{ error: 'claimed_by_other' }`. The endpoint does NOT leak the other claimant's `line_user_id` or display name.
9. **Rate limit:** 10 req/s per `iss` claim. Exceeded → 429 with `Retry-After: 1`. Use an in-memory token bucket (LRU cache keyed on `iss`); acceptable for single-region deployment.
10. **Observability:** every request logs `{ correlation_id (from `X-Correlation-Id` header or fresh UUID), batch_code, status_code, latency_ms }`. Errors capture in Sentry with the correlation id attached.
11. **Token refresh helper:** `POST /api/v1/auth/token` accepts a client-credentials body `{ grant_type: 'client_credentials', client_id, client_secret }`, validates against `LINE_BOT_CLIENT_ID` + `LINE_BOT_CLIENT_SECRET` env vars, returns a 15-min ES256 JWT signed with `CRM_JWT_PRIVATE_KEY`. This is a server-side helper used by the LINE bot at boot — not a user-facing endpoint.

## Tasks / Subtasks

- [x] Task 1 — Shared JWT verification middleware (AC: #2, #11)
  - [x] Create `lib/jwt/verify-line-bot-jwt.ts` exporting `verifyLineBotJwt(req: NextRequest): Promise<{ ok: true, claims: { iss, aud, exp } } | { ok: false, status: 401, code: string }>`
  - [x] Use `jose` (already in the dep tree if installed for Supabase JWT; otherwise `pnpm add jose`); verify ES256 signature against `CRM_JWT_PUBLIC_KEY` env var (PEM, multiline via base64 or `\n` escapes — match the existing pattern for Supabase keys)
  - [x] Create `app/api/v1/auth/token/route.ts` for the refresh endpoint per AC #11; sign with `CRM_JWT_PRIVATE_KEY`
- [x] Task 2 — Read endpoint (AC: #1, #3, #4, #5, #6, #7, #8, #10)
  - [x] Create `app/api/v1/batches/[code]/route.ts` with a `GET` handler
  - [x] Validate `:code` regex per AC #3 before any DB hit; reject 400 fast
  - [x] Call JWT middleware; on failure return the appropriate 401 response
  - [x] Service-role Supabase client (RLS bypassed — this is service-to-service) reads the batch row by `batch_code`, joins `pcr_results` (4 rows) and `batch_certs` (latest URL)
  - [x] Apply the publish/expiry/claim ladder per AC #5–#8
  - [x] Sign the `pcr_certificate_url` with a 1-hour-expiry signed URL via `getSignedPcrCertUrl()` from `lib/supabase/storage.ts` (Supabase Storage `createSignedUrl`) — NOT the raw public URL — so farmers can fetch the cert without authenticating to Supabase. Note: `lib/supabase/storage.ts` already exists but only exports `uploadNurseryLogo` / uses `getPublicUrl`; `getSignedPcrCertUrl(batchId, ttlSeconds=3600)` is a NEW export to be added to that file (see Files to Touch)
  - [x] Log + Sentry per AC #10; include `X-Correlation-Id` in the response
- [x] Task 3 — Rate limit (AC: #9)
  - [x] Create `lib/jwt/rate-limit.ts` with an LRU + token bucket keyed on `iss` claim
  - [x] 10 tokens/sec; refill rate 10/sec; bucket size 10 (allows brief bursts)
  - [x] Return 429 with `Retry-After: 1` on exhaustion
- [x] Task 4 — Env + docs
  - [x] Add to `.env.example`: `CRM_JWT_PUBLIC_KEY` (PEM, comment: "ES256 public key matching LINE bot's CRM_JWT_PRIVATE_KEY"), `CRM_JWT_PRIVATE_KEY`, `LINE_BOT_CLIENT_ID`, `LINE_BOT_CLIENT_SECRET`
  - [x] Add notes to `CLAUDE.md` under the environment-variables table for the new keys (kept short — one line each)
- [x] Task 5 — Tests
  - [x] `tests/api/batches-read.test.ts`: happy path (200), invalid JWT (401), expired JWT (401), wrong format `:code` (400), unknown code (404), unpublished code (404 same body), expired batch (410), rate-limit exhaustion (429), correlation id round-trip (echoed in response header)
  - [x] `tests/api/auth-token.test.ts`: valid client creds → 200 with 15-min JWT; bad secret → 401
  - [x] `pnpm typecheck && pnpm lint && pnpm vitest run tests/api`

## Dev Notes

### Architecture Constraints

- This route lives **outside** `app/[locale]/(dashboard)/...` because it has no locale and no UI. Place under `app/api/v1/` per Next.js App Router conventions; no `BillingGate`, no `next-intl` middleware. The root middleware file is `proxy.ts` (repo root, not `lib/supabase/middleware.ts` which is the Supabase auth helper). Its matcher is `/((?!api|auth|_next|_vercel|.*\\..*).*)` (proxy.ts:17), which excludes `/api/*` via negative lookahead — confirmed; `/api/v1/batches/[code]` will not be processed by `next-intl` middleware.
- Service-role Supabase client only. Do not use the cookie-bound client; this endpoint has no user session.
- ES256 (not HS256) because the LINE bot is a separate service that should not hold the CRM's signing secret. Asymmetric keys → CRM signs with private, LINE bot verifies with public, and vice versa for the webhook direction in Story K4.
- No `requireActiveSubscription()` here — batch-read is part of the core nursery-to-farmer trust loop and must not be gated by Stripe state. Locked behind `published_at` (operator opt-in) instead.
- Strict `text/json; charset=utf-8` content type. No HTML error pages.

### Files to Touch

| File | Action |
|------|--------|
| `app/api/v1/batches/[code]/route.ts` | CREATE |
| `app/api/v1/auth/token/route.ts` | CREATE |
| `lib/jwt/verify-line-bot-jwt.ts` | CREATE |
| `lib/jwt/sign-line-bot-jwt.ts` | CREATE — used by token endpoint |
| `lib/jwt/rate-limit.ts` | CREATE — LRU + token bucket |
| `lib/supabase/storage.ts` | EDIT — add `getSignedPcrCertUrl(batchId, ttlSeconds=3600)` |
| `.env.example` | EDIT — new keys |
| `CLAUDE.md` | EDIT — env-var table |
| `tests/api/batches-read.test.ts` | CREATE |
| `tests/api/auth-token.test.ts` | CREATE |

### API Facade Methods

This story does NOT route through `lib/api/index.ts` — the API facade is for *dashboard* data flow. Service-to-service endpoints are independent.

### RBAC

- No `can()` checks. JWT `iss=line-bot` is the only authorisation gate.
- Future: if a second downstream service (e.g., a nursery-side LINE bot) requests this endpoint, extend `iss` whitelist; until then reject any other `iss`.

### RLS Tables

| Table | Operation | Who |
|------|-----------|-----|
| `batches` | SELECT by `batch_code` | service-role |
| `pcr_results` | SELECT joined by `batch_id` | service-role |
| `batch_certs` | SELECT joined by `batch_id` | service-role |
| `batch_claims` | SELECT for the "claimed_by_other" check | service-role |

### Persona / Voice

- All response bodies are machine-readable JSON. No Thai strings, no persona voice. (The LINE bot wraps results in `น้องน้ำ` voice on the farmer-facing side.)

### Testing

```bash
pnpm vitest run tests/api/batches-read.test.ts tests/api/auth-token.test.ts
pnpm typecheck
pnpm lint
```

### References

- [Source: docs/bmad/stories/_integration-with-line-bot-Epic-K.md]
- [Source: docs/bmad/stories/K1.batch-code-and-claims-schema.md]
- [Source: aquawise-line-bot/docs/bmad/architecture.md — ADR-018 §1, §2, §5, §6]
- [Source: aquawise-line-bot/docs/bmad/stories/K1.batch-code-claim-flow.md — consumer]
- [Source: aquawise-line-bot/docs/bmad/stories/K7.pcr-certificate-flex.md — consumer of `pcr_certificate_url`]

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — deep-executor (WS4 Epic K, 2026-05-17)

### Debug Log References

### Completion Notes List

- 2026-05-17 — DONE. GET /api/v1/batches/[code] (route.ts) + /api/v1/auth/token (client-credentials refresh). ES256 verify via vendored verifyFlowToken (read-claim flow, aud=hatchery-crm). Ladder: 400 invalid_code_format → 401 (missing/bad/expired, WWW-Authenticate on expiry) → 429 rate-limit → 404 unknown==unpublished (no draft leak) → 410 expired → 200 contract wire shape (hatchery_* names per vendored contract-types). Signed PCR cert URL via getSignedPcrCertUrl. Live curl verified: token-refresh→200 JWT, read→200 full shape, 404, 401. Green: pnpm typecheck+lint clean, pnpm test 227 passed (floor 177, never regressed). SHA: see docs/temp-docs/STORY-CHECKLIST.md.

### File List
