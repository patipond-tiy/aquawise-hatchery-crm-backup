# Story C1: Register a New Batch

Status: ready-for-dev

## Story

As a Manager (P2) or Lab Officer (P4),
I want to register a new PL batch with source strain, spawn date, quantity, and PCR results in under 60 seconds,
so that the batch is ready to assign to buyers and documented with verified quality data.

## Acceptance Criteria

1. A 3-step wizard is presented: (1) batch info, (2) PCR upload, (3) confirm and register.
2. Step 1 collects: source strain (dropdown — options sourced dynamically from a `strains` reference table or whichever source the codebase already provides; the list is not a hardcoded set of 4 strings), spawn date (date picker), and PL count (number input). All fields are required.
3. Step 2 accepts a PDF, JPG, or PNG file drop/select. The file is uploaded to the `pcr-reports/{batch_id}/` Storage bucket. Per-disease results (4 diseases) are captured via a form the user can fill in or override; the current hardcoded "✓ ผ่าน" placeholders are replaced with real controlled inputs.
4. Step 3 shows a confirmation summary of all entered data before final submission.
5. On submit, a `batches` row and one `pcr_results` row per disease are inserted atomically. If the `pcr_results` insert fails the `batches` insert is rolled back.
6. A success toast is shown and the batch list is invalidated (TanStack Query).
7. RBAC enforcement: `counter_staff` may insert a batch row but may NOT insert `pcr_results` rows; `lab_tech` may insert `pcr_results` rows; `owner` may do both. The wizard enforces this at the server action level — not just in the UI.
8. If the PCR file upload fails, the wizard surfaces an error on step 2 and does not advance to step 3.
9. **(LINE bot Epic K integration)** On successful registration, the DB-side `mint_batch_code()` function (Story K1) generates a 6-char unguessable `batch_code` of the form `B-XXXXXX`. The success toast shows this code so the operator can print it on the delivery receipt: `เพิ่มล็อต {batch_id} แล้ว — รหัสล็อตสำหรับฟาร์ม: {batch_code}`. A new "เผยแพร่ล็อตให้ฟาร์มสแกน" toggle on step 3 sets `batches.published_at = now()` and freezes the current hatchery contact (LINE OA + public phone — Story A3 mod) into `batches.hatchery_contact_snapshot`. If toggled off, `published_at` stays NULL and the LINE bot's `GET /api/v1/batches/:code` returns 404 (per Story K2 AC #6) — the code exists but is not claimable.
10. **(LINE bot Epic K integration)** A "วันหมดอายุรหัส" date picker on step 3 sets `valid_until` (default `created_at + 30 days`, capped at 90 days by the CHECK constraint from K1). The wizard does not allow values outside the allowed range.

> **Dependency note:** C1 must be completed before C3 (real buyers/PCR data) and C4 (cert generation) can be fully implemented. These downstream stories are explicitly blocked on C1.i. AC #9 and #10 depend on Story K1 (schema migration) landing first.

## Tasks / Subtasks

- [ ] Confirm `pcr_results` column shape from migration 001 before writing any insert code (AC: #5)
  - [ ] Read `supabase/migrations/001_init.sql` and document all columns of `pcr_results`
  - [ ] Ensure no new migration is required for the table itself — only the Storage bucket may need provisioning

- [ ] Create/confirm the `pcr-reports` Storage bucket with tenant-scoped RLS (AC: #3)
  - [ ] Add bucket creation to a new migration or confirm it is already provisioned (check migration 012)
  - [ ] RLS: authenticated user's `hatchery_id` must match the batch's `hatchery_id`

- [ ] Wire step 2 file upload in `add-batch-modal.tsx` to Supabase Storage (AC: #3, #8)
  - [ ] Add upload logic to `lib/supabase/storage.ts` (follow the existing `uploadHatcheryLogo` pattern)
  - [ ] Use `{batch_id}` as a path prefix; generate a temporary UUID if `batch_id` is not yet persisted
  - [ ] Surface upload errors in the step 2 form state; block advance to step 3 on failure

- [ ] Replace hardcoded "✓ ผ่าน" disease results with a real per-disease form (AC: #3)
  - [ ] 4 disease inputs with pass/fail toggle and optional note field
  - [ ] Form state managed with `useState`; values forwarded to the server action payload

- [ ] Create server action `addBatch()` at `app/[locale]/(dashboard)/batches/actions.ts` (AC: #5, #6, #7)
  - [ ] Accept `batches` fields + array of per-disease PCR results + `pcr_file_url`
  - [ ] Check caller role via `can(role, 'batch:write')` for the batch row; check `can(role, 'pcr:write')` for PCR rows (requires adding `pcr:write` to `lib/rbac.ts` — see RBAC section below)
  - [ ] Insert `batches` row first; on success insert all `pcr_results` rows; on PCR insert failure delete the batch row (or use a Postgres function/RPC for atomicity)
  - [ ] Call `requireActiveSubscription()` from `lib/billing/guard.ts` at entry

- [ ] Update strain dropdown to read from a real source rather than 4 hardcoded strings (AC: #2)
  - [ ] Determine whether strains live in a `prices` table or a new `strains` table; use whichever the codebase already has

- [ ] Wire step 3 confirmation summary to display all wizard state (AC: #4)
  - [ ] Show: strain, spawn date, PL count, disease results, PCR file name

- [ ] Invalidate batch list query on success and show Thai-language toast (AC: #6)
  - [ ] Use TanStack Query `queryClient.invalidateQueries` with the batches query key
  - [ ] Toast: `เพิ่มล็อต "{batchId}" แล้ว`

- [ ] (Epic K) Wire the "เผยแพร่ล็อตให้ฟาร์มสแกน" toggle + "วันหมดอายุรหัส" picker on step 3 (AC: #9, #10)
  - [ ] Read `hatcheries.line_oa_id` and `hatcheries.contact_phone_public` (Story A3 mod columns); if either is missing, disable the publish toggle and surface a Thai hint: `"ตั้งค่าข้อมูลติดต่อ (LINE OA / เบอร์) ในหน้าตั้งค่าก่อนจึงจะเผยแพร่ได้"`
  - [ ] On submit with toggle ON: set `batches.published_at = now()`, snapshot the two contact fields into `batches.hatchery_contact_snapshot` jsonb (`{ line_oa_id, contact_phone_public, hatchery_name_th }`)
  - [ ] On submit with toggle OFF: leave both columns NULL — the batch is registered but not claimable from the LINE bot until a separate "เผยแพร่ตอนนี้" action is invoked on the batch detail page (a follow-up to C3 mod)
  - [ ] Surface the generated `batch_code` in the success toast (AC #9 copy)
  - [ ] In mock mode, generate a placeholder `batch_code` using the same alphabet (`B-` + 6 chars from `ABCDEFGHJKMNPQRSTUVWXYZ23456789`); mock does NOT round-trip to the live `mint_batch_code()` function

- [ ] Write Vitest tests at `tests/batches/register.test.ts` (AC: #5, #7, #8)
  - [ ] Missing PCR file → wizard blocks step advance
  - [ ] Atomic rollback when `pcr_results` insert fails
  - [ ] `counter_staff` role cannot insert `pcr_results` (server action rejects)
  - [ ] `lab_tech` role can insert `pcr_results`

## Dev Notes

### Architecture Constraints
- All API calls via `lib/api/index.ts` facade; never call Supabase directly from a page or modal
- Server action lives at `app/[locale]/(dashboard)/batches/actions.ts`
- `'use client'` opt-in only; the modal already uses client state, mark it explicitly
- No `any` in app code; use `Database['public']['Tables']['pcr_results']['Insert']` for the insert type
- Call `requireActiveSubscription()` from `lib/billing/guard.ts` at the top of every mutation server action

### Files to Touch
- `components/modals/add-batch-modal.tsx` — wire real PCR form and file upload
- `app/[locale]/(dashboard)/batches/actions.ts` — create (new file)
- `lib/supabase/storage.ts` — add `uploadPcrReport(batchId, file)` helper
- `lib/api/index.ts` — expose `addBatch()` if not already present
- `lib/api/supabase.ts` — implement `addBatch()` live path
- `lib/mock/api.ts` — update mock `addBatch()` to persist real PCR structure
- `supabase/migrations/00X_pcr_bucket.sql` — if Storage bucket not yet in a migration
- `tests/batches/register.test.ts` — new test file

### API Facade Methods
- `addBatch(input: AddBatchInput): Promise<Batch>` — existing mock stub, needs live implementation
- `uploadPcrReport(batchId: string, file: File): Promise<string>` — new; returns public/signed URL

### RBAC
- `can(role, 'batch:write')` — gates batch row insert (counter_staff, lab_tech, owner)
- **PCR rows require finer-grained gating.** Today `batch:write` includes `counter_staff`, but AC #7 says counter_staff must NOT insert `pcr_results`. Two options:
  1. **(Recommended)** Add a `pcr:write` action to `lib/rbac.ts` granted to `['owner', 'lab_tech']` only. Gate the PCR insert with `can(role, 'pcr:write')`.
  2. Alternatively, hardcode the PCR check as `role === 'owner' || role === 'lab_tech'` in the server action — but this bypasses the `can()` pattern.
- Use `lib/rbac.ts` `can()` function in the server action; do not branch on role strings directly

### RLS Tables
- `batches` — migration 001; `hatchery_id` RLS on insert
- `pcr_results` — migration 001; confirm exact column names before writing insert (see critical note below)
- Storage bucket `pcr-reports` — may need a new migration; check 012

> **CRITICAL:** `pcr_results` is the correct table name. `batch_pcr_tests` does NOT exist and was a documentation drift resolved in migration 001 (see MATRIX.md drift flag). Confirm the column shape by reading migration 001 before writing any insert code.

> **Source-anchor drift note (do NOT treat as a story error):** The `[Source: docs/product-spec/03-user-stories.md#C1]` anchor at the bottom of this file resolves to legacy text in `03-user-stories.md` that still reads `batch_pcr_tests`. This story file is correct — it uses `pcr_results` throughout. The drift is in the source doc, not here. Recommendation: update `03-user-stories.md#C1` to replace `batch_pcr_tests` with `pcr_results` (see orchestrator recommendation in Worker C results).

### i18n Keys
Add to both `messages/th.json` and `messages/en.json`:
- `batches.wizard.step1` — "ข้อมูลล็อต" / "Batch Info"
- `batches.wizard.step2` — "ผลตรวจ PCR" / "PCR Results"
- `batches.wizard.step3` — "ยืนยัน" / "Confirm"
- `batches.wizard.pcrUpload` — "อัปโหลดรายงาน PCR" / "Upload PCR Report"
- `batches.wizard.diseaseResult` — "ผ่าน / ไม่ผ่าน" / "Pass / Fail"
- `batches.toast.registered` — "เพิ่มล็อต "{id}" แล้ว" / "Batch {id} registered"
- `batches.error.pcrRequired` — "กรุณาอัปโหลดผล PCR ก่อนดำเนินการต่อ" / "Please upload PCR results before continuing"
- `batches.error.unauthorized` — "บทบาทของคุณไม่มีสิทธิ์บันทึกผล PCR" / "Your role cannot submit PCR results"

### Testing
```bash
pnpm vitest run tests/batches/register.test.ts
pnpm typecheck && pnpm lint
```

### References
- [Source: docs/product-spec/03-user-stories.md#C1]
- [Source: docs/work-breakdown/MATRIX.md#C1]
- [Source: docs/bmad/stories/K1.batch-code-and-claims-schema.md — `mint_batch_code()`, `valid_until`, `published_at`, `hatchery_contact_snapshot`]
- [Source: docs/bmad/stories/A3.set-up-hatchery-profile.md — `line_oa_id`, `contact_phone_public` fields referenced by AC #9]
- [Source: aquawise-line-bot/docs/bmad/stories/K1.batch-code-claim-flow.md — downstream consumer of `batch_code`]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
