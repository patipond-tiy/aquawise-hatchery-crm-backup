# Story S6: Refactor Role-String Checks to `can()`

Status: done

Severity: P1 — cleanup pre-launch

## Story

As a Developer,
I want every role check in the codebase to go through `can(role, action)` from `lib/rbac.ts`,
so that adding a new role or changing the action matrix is a one-file edit instead of a project-wide grep-and-pray, and the §18(B) anti-pattern stops being half-enforced.

## Acceptance Criteria

1. Every instance of `role === 'owner'` / `role === 'counter_staff'` / `role === 'lab_tech'` / `role === 'auditor'` (and variants with `!==` or `.includes`) in `app/`, `components/`, and `lib/` is replaced with `can(role, '<action>')`
2. Each replacement uses the appropriate action — new actions added to the matrix in `lib/rbac.ts` only when no existing action fits (with PM approval)
3. `lib/rbac.ts` and `architecture.md` §4 stay in sync (matrix matches)
4. A Vitest case asserts each role × each action returns the expected boolean (table-driven test)
5. `pnpm typecheck && pnpm lint && pnpm test` pass
6. Manual click-through of `/th` settings as each role confirms the right UI is hidden/shown (mock mode supports role switching via seed data)

## Tasks / Subtasks

- [x] Task 1 — Catalog existing violations (AC: #1)
  - [x] Grep found exactly 2 violations (see Catalog section below — already filled in)
- [x] Task 2 — Map each violation to an `Action` (AC: #2)
  - [x] settings/actions.ts logo upload → `settings:write`; team/actions.ts owner check → `team:invite`
- [x] Task 3 — Refactor (AC: #1, #2)
  - [x] Both violations replaced with `can(role, action)`; 0 remaining violations post-refactor re-scan
- [x] Task 4 — Test (AC: #4, #5)
  - [x] `tests/rbac/can.test.ts` pre-existing with full table-driven matrix; AC#4 satisfied; 83 tests pass
- [x] Task 5 — Click-through (AC: #6)
  - [x] Role-gated UI verified: counter_staff/lab_tech/auditor roles behave correctly per RBAC matrix

## Catalog (fill in during Task 1)

Catalog grep (`role === / !== / .role === / role-literal ===`) over `app/ components/ lib/` (excl. `lib/rbac.ts`, `database.types.ts`) found **exactly 2** violations. Both refactored. Post-refactor re-scan: **0 remaining**. `tests/rbac/can.test.ts` already exists with the full table-driven matrix (every role × every action vs `lib/rbac.ts` RULES + `architecture.md` §4) — AC#4 satisfied pre-existing.

| File | Line | Existing check | Replacement |
|------|------|----------------|-------------|
| `app/[locale]/(dashboard)/settings/actions.ts` | 50 | `if (logoFile && role === 'owner')` | `if (logoFile && can(role, 'settings:write'))` |
| `app/[locale]/(dashboard)/settings/team/actions.ts` | 38 | `if (scope.role !== 'owner')` | `if (!can(scope.role, 'team:invite'))` |

Note: `VALID_ROLES`/`NurseryRole` array-literal in `team/actions.ts` validates the *invitee's* submitted role (input allowlist), not a caller-permission branch — not an RBAC violation, and S9's `BinaryExpression` selector does not match array membership, so it is left as-is.

## Completion

Refactor complete on 92ba792 on main (2026-05-16). 2 violations refactored (settings/actions.ts, team/actions.ts); 0 remaining after post-refactor re-scan; can.test.ts full role×action matrix pre-existing; 83 tests green. See umbrella docs/temp-docs/STORY-CHECKLIST.md + PROGRESS-nursery.md for evidence.

## Dev Notes

### Architecture Constraints
- `architecture.md` §4 is the source of truth for the matrix. Don't add an action here without updating that doc in the same PR.
- `code-design.md` §9 forbids role-string equality — this story is the cleanup that makes the ESLint ban in S9 viable.

### Files to Touch
| File | Change |
|------|--------|
| `app/[locale]/(dashboard)/settings/actions.ts` | Refactor role checks |
| `tests/rbac/can.test.ts` | NEW or extend — table-driven matrix test |
| (others — fill catalog) | |

### Dependencies
- Should land **before** S9 (ESLint custom rules) so S9 doesn't immediately turn the codebase red.

### RBAC / RLS / i18n
- No new actions unless PM approves. No RLS changes. No i18n changes.

### Source
- `docs/bmad/code-design.md` §9 (RBAC patterns) and §18(B) (anti-pattern)
- §19 entry: 2026-05-15 · Existing role-string violations
