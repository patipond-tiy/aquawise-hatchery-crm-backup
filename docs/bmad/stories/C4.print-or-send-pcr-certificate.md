# Story C4: Print or Send PCR Certificate

Status: ready-for-dev

## Story

As a Manager (P2),
I want to print a PCR certificate as a PDF or send it as a LINE Flex Message to the buyer,
so that the farm has documented proof of the lot's quality.

## Acceptance Criteria

1. "พิมพ์ใบรับรอง" triggers server-side PDF generation and initiates a browser download. The PDF is rendered using `@react-pdf/renderer` (not Puppeteer) for Next.js compatibility.
2. The PDF includes: hatchery logo, hatchery name, batch ID, spawn date, source strain, per-disease PCR results (disease name, pass/fail, lab name, test date), a "Verified by AquaWise" stamp, and the certificate generation date.
3. Brand-tier typography: Plus Jakarta Sans for Latin text, Noto Sans Thai for Thai text, no emoji anywhere in the PDF. No marketing copy.
4. On successful PDF generation, the cert URL is stored in a new `batch_certs` table for re-download (idempotent: if a `batch_certs` row already exists for this `batch_id` and no `pcr_results` row for this batch has `updated_at` greater than the existing `batch_certs.generated_at`, return the existing record rather than re-generating. Re-generation is triggered only when at least one `pcr_results` row has `updated_at > batch_certs.generated_at`).
5. "ส่งใบรับรอง LINE" opens the existing `cert-modal.tsx`, listing the batch's buyers (from `batch_buyers`). The manager selects one or more recipients and submits.
6. On submit, one row per selected recipient is inserted into `line_outbound_events` with template `pcr_certificate` — idempotent on `(batch_id, customer_id)` partial unique index.
7. The LINE send path is **blocked by G3'.i** (bot worker not yet built). The modal submission can be wired and the `line_outbound_events` rows can be inserted, but actual LINE delivery will not occur until G3' lands. This must be clearly communicated in the modal UI ("จะส่งเมื่อระบบ LINE พร้อม" / "Will be sent when LINE system is ready").
8. RBAC: only `owner` and `lab_tech` may generate a certificate (cert generation touches `pcr_results`). `counter_staff` may send an existing cert via LINE but may not generate a new one. `auditor` may not perform either action.

> **Dependency note:** C4 is blocked on C1.i (real `pcr_results` rows must exist) and C3.i (distribution data needed for buyer list). The LINE send path is additionally blocked on G3'.i. Implement C1 and C3 first.

## Tasks / Subtasks

- [ ] Create a new migration for the `batch_certs` table (AC: #4)
  - [ ] New file: `supabase/migrations/014_batch_certs.sql`
  - [ ] Schema: `batch_certs(id uuid PK default gen_random_uuid(), batch_id uuid NOT NULL REFERENCES batches(id), pdf_url text NOT NULL, generated_at timestamptz NOT NULL default now(), generated_by uuid REFERENCES auth.users(id))`
  - [ ] Unique constraint on `batch_id` to enforce one-cert-per-batch idempotency (or a unique index if soft re-generation is needed — decide before writing)
  - [ ] RLS: `hatchery_id`-scoped read (via `batches` FK); write restricted to `owner` and `lab_tech` roles
  - [ ] Provision the `pcr-certificates` Storage bucket with folder prefix `{batch_id}/`; add to this migration or confirm it is covered by an existing migration

- [ ] Create `lib/pdf/cert.tsx` with the `@react-pdf/renderer` certificate component (AC: #1, #2, #3)
  - [ ] Install `@react-pdf/renderer` if not already in `package.json`; confirm it is listed as a dependency (not devDependency)
  - [ ] Define a `PcrCertDocument` React component using `@react-pdf/renderer` primitives (`Document`, `Page`, `View`, `Text`, `Image`)
  - [ ] Layout: hatchery logo at top-left; "ใบรับรองคุณภาพลูกกุ้ง" (Thai) / "Shrimp PL Quality Certificate" (English) as title; batch metadata block; per-disease PCR results table; "Verified by AquaWise" footer stamp; generation date in Buddhist Era (Thai locale)
  - [ ] Font registration: register Plus Jakarta Sans (Latin) and Noto Sans Thai; source files from `public/fonts/` — confirm which font files are already present before adding new ones
  - [ ] No emoji anywhere in the document. No exclamation marks. Voice: professional, deferential, scientific (per `docs/product-spec/07-brand-and-voice.md` brand-surface tier)
  - [ ] Export `renderPcrCertPdf(batchDetail: BatchDetail, hatchery: Hatchery): Promise<Buffer>` — a server-only function that renders and returns the PDF as a `Buffer`

- [ ] Create server action `generatePcrCertPdf(batchId)` in `app/[locale]/(dashboard)/batches/actions.ts` (AC: #1, #4, #8)
  - [ ] Mark the file `'server only'` — this action must never bundle into the client
  - [ ] Call `requireActiveSubscription()` from `lib/billing/guard.ts` at entry
  - [ ] Check caller role via `can(role, 'pcr:write')` — only `owner` and `lab_tech` may generate; reject `counter_staff` and `auditor` with a typed error (requires `pcr:write` action from C1)
  - [ ] Fetch `BatchDetail` via `getBatch()`; fetch `Hatchery` (for logo, name, brand)
  - [ ] Call `renderPcrCertPdf(batchDetail, hatchery)` from `lib/pdf/cert.tsx`
  - [ ] Upload the resulting buffer to `pcr-certificates/{batch_id}/{generated_at_iso}.pdf` via Supabase Storage; obtain a signed URL (or public URL if the bucket is public)
  - [ ] Upsert a row into `batch_certs`; if a row for this `batch_id` already exists and no `pcr_results` row for this batch has `updated_at > batch_certs.generated_at`, return the existing `pdf_url` without re-rendering (idempotent per AC#4)
  - [ ] Return `{ pdfUrl: string }` to the caller; the client triggers `window.open(pdfUrl, '_blank')` for download

- [ ] Create server action `sendCertificate(batchId, customerIds)` in `app/[locale]/(dashboard)/batches/actions.ts` (AC: #5, #6, #7, #8)
  - [ ] Call `requireActiveSubscription()` at entry
  - [ ] Check caller role: `owner`, `lab_tech`, and `counter_staff` may send an existing cert; `auditor` may not
  - [ ] For each `customerId` in `customerIds`, insert one row into `line_outbound_events` with `template = 'pcr_certificate'`, `payload` matching the contract in `docs/product-spec/05-line-integration.md`: `{ hatchery_id, customer_id, batch_id, cert_url, summary: {WSSV, EHP, IHHNV, TSV} }` — use snake_case keys; `cert_url` (not `pdfUrl`); include the per-disease summary object so the bot can render inline PCR status in the Flex card
  - [ ] Idempotency: use `ON CONFLICT (batch_id, customer_id) DO NOTHING` on the `line_outbound_events` partial unique index (confirm index name from migration 006 before writing)
  - [ ] Return `{ enqueued: number }` — the count of newly inserted rows (0 if all were duplicates)
  - [ ] Note clearly in a server-side log: "LINE send enqueued — delivery pending G3'.i bot worker"

- [ ] Wire `cert-modal.tsx` submission to `sendCertificate()` server action (AC: #5, #7)
  - [ ] The modal currently opens but has no submit handler — wire it via `useTransition` + the server action
  - [ ] Populate the buyer list in the modal from `batchDetail.distributions` (passed as a prop or fetched via a query inside the modal)
  - [ ] Add a checkbox list of buyers; require at least one selection before enabling the submit button
  - [ ] Show a non-alarming informational note in Thai: "จะส่งเมื่อระบบ LINE พร้อม" below the submit button, consistent with ลูกหลานที่เรียนมา voice (calm, declarative — no urgency, no emoji)
  - [ ] On success, show Thai toast "เพิ่มคิวส่งใบรับรองให้ {count} ราย แล้ว" and close the modal

- [ ] Wire the "พิมพ์ใบรับรอง" button to `generatePcrCertPdf()` (AC: #1)
  - [ ] Replace the current toast-only handler with a call to the server action
  - [ ] Show a loading state ("กำลังสร้างใบรับรอง...") while the server action is in flight; use `useTransition`
  - [ ] On success, open the returned `pdfUrl` in a new tab
  - [ ] On error, show a toast with the error message (Thai); do not expose internal error details

- [ ] Write Vitest tests at `tests/batches/cert.test.ts` (AC: #1, #6, #8)
  - [ ] PDF byte snapshot: `renderPcrCertPdf()` with a canonical `BatchDetail` fixture returns a Buffer whose first 4 bytes are `%PDF` (valid PDF magic bytes)
  - [ ] `counter_staff` role calling `generatePcrCertPdf()` server action returns a role-rejection error (does not generate PDF)
  - [ ] `lab_tech` role calling `generatePcrCertPdf()` succeeds (mock path)
  - [ ] `sendCertificate()` with the same `(batchId, customerId)` twice inserts only one row into `line_outbound_events` (idempotency assertion)
  - [ ] `sendCertificate()` with `auditor` role returns a role-rejection error

## Dev Notes

### Architecture Constraints
- `lib/pdf/cert.tsx` must be `'server only'` — `@react-pdf/renderer` renders on the server; do not import it in any client bundle
- Server action lives at `app/[locale]/(dashboard)/batches/actions.ts` (may already be partially created for C1 — extend, do not create a duplicate)
- `'use client'` opt-in only; `cert-modal.tsx` is already a client component; the print button handler needs client marking if it uses `useTransition`
- No `any` in app code; type the `@react-pdf/renderer` components using their exported types
- Call `requireActiveSubscription()` from `lib/billing/guard.ts` at the top of every mutation server action
- The LINE send path (G3') is in a separate repo (`aquawise-line-bot`, Cloud Run); this story only inserts into `line_outbound_events` — it does not call LINE directly

### Files to Touch
- `app/[locale]/(dashboard)/batches/actions.ts` — add `generatePcrCertPdf()` and `sendCertificate()` (create file if it does not exist yet; C1.i may have created it)
- `lib/pdf/cert.tsx` — new file; server-only PDF renderer
- `components/modals/cert-modal.tsx` — wire submit handler; add buyer checkbox list; add LINE-pending notice
- `supabase/migrations/014_batch_certs.sql` — new migration
- `lib/database.types.ts` — regenerate after migration
- `lib/api/supabase.ts` — add `getBatchCert(batchId)` and `upsertBatchCert(cert)` helpers if not inline in the action
- `lib/api/index.ts` — expose any new methods
- `tests/batches/cert.test.ts` — new test file
- `public/fonts/` — confirm Plus Jakarta Sans + Noto Sans Thai font files are present; add if absent

### API Facade Methods
- `getBatchCert(batchId: string): Promise<BatchCert | null>` — new; checks for existing cert record
- `upsertBatchCert(cert: BatchCertInsert): Promise<BatchCert>` — new; writes cert record after upload
- `enqueueCertSend(batchId: string, customerIds: string[]): Promise<{ enqueued: number }>` — new; inserts `line_outbound_events` rows

### RBAC
- `can(role, 'pcr:write')` — gates `generatePcrCertPdf()` (cert generation requires PCR write access: `owner`, `lab_tech` only). **Note:** `pcr:write` must be added to `lib/rbac.ts` as part of C1 (see C1 RBAC notes). If C1 has not yet landed, use `can(role, 'batch:write')` as a temporary gate but add a hardcoded `role !== 'counter_staff'` check until `pcr:write` exists.
- `sendCertificate()` requires `can(role, 'customer:write')` (grants `owner`, `counter_staff`) **OR** `can(role, 'pcr:write')` (grants `owner`, `lab_tech`). The union of these two covers `owner`, `counter_staff`, and `lab_tech` — excluding `auditor`. Use `can(role, 'customer:write') || can(role, 'pcr:write')` as the gate. Do NOT use `batch:read` alone — it includes `auditor`.
- `auditor` may not generate OR send certificates
- Use `lib/rbac.ts` `can()` — do not branch on role strings directly

### RLS Tables
- `batch_certs` — new in migration 014; `hatchery_id`-scoped via `batches` FK; write restricted to owner/lab_tech
- `line_outbound_events` — migration 006; existing idempotency index on `(customer_id, template, cycle_id)` — confirm if `(batch_id, customer_id)` partial unique exists or if a new index is needed for cert idempotency
- `batches` — migration 001; read needed to fetch batch detail for PDF
- `pcr_results` — migration 001; read needed to populate cert content
- Storage bucket `pcr-certificates` — provision with prefix `{batch_id}/`; tenant-scoped RLS matching `pcr-reports` pattern

> **CRITICAL LINE DEPENDENCY:** The LINE send path (`sendCertificate()` → `line_outbound_events` → bot worker → farmer LINE) will NOT deliver messages until G3'.i (bot worker) is built and deployed. Do not represent this as "sending" in any user-facing string. Use "จะส่งเมื่อระบบ LINE พร้อม" (calm declarative) — never "กำลังส่ง" (implying immediate delivery).

> **PDF LIBRARY CHOICE:** Use `@react-pdf/renderer` — not Puppeteer, not `html2pdf`, not `jsPDF`. Puppeteer requires a full Chromium install which is incompatible with Next.js server actions in serverless/edge environments. `@react-pdf/renderer` runs purely in Node.js with no browser dependency.

### i18n Keys
Add to both `messages/th.json` and `messages/en.json`:
- `batches.cert.printButton` — "พิมพ์ใบรับรอง" / "Print Certificate"
- `batches.cert.sendButton` — "ส่งใบรับรอง LINE" / "Send Certificate via LINE"
- `batches.cert.generating` — "กำลังสร้างใบรับรอง..." / "Generating certificate..."
- `batches.cert.modal.title` — "ส่งใบรับรอง PCR" / "Send PCR Certificate"
- `batches.cert.modal.selectBuyers` — "เลือกผู้รับ" / "Select recipients"
- `batches.cert.modal.linePending` — "จะส่งเมื่อระบบ LINE พร้อม" / "Will be sent when LINE system is ready"
- `batches.cert.modal.submit` — "เพิ่มคิวส่ง" / "Enqueue send"
- `batches.cert.modal.selectRequired` — "กรุณาเลือกผู้รับอย่างน้อย 1 ราย" / "Please select at least one recipient"
- `batches.cert.toast.enqueued` — "เพิ่มคิวส่งใบรับรองให้ {count} ราย แล้ว" / "Certificate send queued for {count} recipient(s)"
- `batches.cert.toast.generated` — "สร้างใบรับรองล็อต {id} แล้ว" / "Certificate for batch {id} generated"
- `batches.cert.error.unauthorized` — "บทบาทของคุณไม่มีสิทธิ์สร้างใบรับรอง" / "Your role cannot generate certificates"
- `batches.cert.pdf.title` — "ใบรับรองคุณภาพลูกกุ้ง" / "Shrimp PL Quality Certificate"
- `batches.cert.pdf.verifiedBy` — "รับรองโดย AquaWise" / "Verified by AquaWise"

### Testing
```bash
pnpm vitest run tests/batches/cert.test.ts
pnpm typecheck && pnpm lint
```

### References
- [Source: docs/product-spec/03-user-stories.md#C4]
- [Source: docs/work-breakdown/MATRIX.md#C4]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
