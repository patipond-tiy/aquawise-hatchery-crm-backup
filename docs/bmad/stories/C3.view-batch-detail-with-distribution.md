# Story C3: View Batch Detail with Distribution

Status: ready-for-dev

## Story

As a Manager (P2),
I want to see who bought from a specific batch and their D30 outcomes,
so that if a complaint comes in I have full context on lineage and performance.

## Acceptance Criteria

1. The batch detail page header shows: batch ID, PCR chip (clean / flagged / pending), spawn date, source strain.
2. A stats row shows: produced PL count, sold PL count, buyer farm count, mean D30. All four values are computed from real data — not hardcoded.
3. The PCR section shows one row per disease with: disease name, pass/fail result, lab name (from `pcr_results.lab` — NOT hardcoded "DOFR"), and test date. If no `pcr_results` rows exist for the batch, render an empty state.
4. The buyers table shows real rows from `batch_buyers` joined with `customers` — not synthetic data. Columns: farm name, quantity (PL), sold date, D30 (from linked `customer_cycles`). If no distribution rows exist, render an empty state.
5. A D30 distribution histogram (10 bins, equal width from 0% to 100%) is rendered using real D30 values from `customer_cycles` linked to this batch. If fewer than 3 data points are available the histogram renders a graceful empty state ("ข้อมูลไม่เพียงพอสำหรับการแสดงกราฟ") rather than a misleading chart. This is the most uncertain feature per source doc §3 Job 2 — the histogram must degrade gracefully; it must not block the page from loading.
6. The page is only accessible after C1 is complete (real `batches` and `pcr_results` rows exist). An invalid or non-existent `id` returns a 404.
7. **(LINE bot Epic K integration)** A new "ฟาร์มที่สแกนรหัสล็อต" panel is rendered below the buyers table. It reads from `batch_claims` (Story K1) and shows: LINE display name (from `line_profile.display_name`), `claimed_at` formatted as Thai date, optional avatar (`line_profile.picture_url`), and a `pond_id` hash truncated to 8 chars. Empty state: `"ยังไม่มีฟาร์มสแกนรหัสล็อตนี้"`. Hide the panel entirely if the batch has `published_at IS NULL` (the batch is not yet claimable, so empty-state would be misleading).
8. **(LINE bot Epic K integration)** A "เผยแพร่ตอนนี้" / "หยุดเผยแพร่" toggle is rendered in the page header next to the PCR chip when `published_at IS NULL` / `published_at IS NOT NULL` respectively. Toggling ON snapshots hatchery contact (same logic as C1 AC #9) and sets `batches.published_at = now()`. Toggling OFF sets `published_at = NULL` — any in-flight claims already recorded are preserved (they remain visible in the panel), but future `GET /api/v1/batches/:code` calls return 404.
9. **(LINE bot Epic K integration)** A "แจ้งเตือนล็อตนี้" button is rendered in the page header (defined fully in Story K4). It opens the publish-warning modal; on success the page refetches and shows the most recent warning under a new "การแจ้งเตือนล็อตนี้" sub-section that lists `crm_event_log` rows for this `batch_id` (severity badge + title_th + posted_at + delivered_at status).

> **Dependency note:** C3 is explicitly blocked on C1.i. AC #7–#9 additionally depend on Stories K1 (schema) and K4 (publish-warning modal). The `pcr_results` and `batch_buyers` tables must have real rows before this page can be fully exercised. Implement and ship C1 first.

## Tasks / Subtasks

- [ ] Confirm schema shape before writing any query (AC: #3, #4)
  - [ ] Read `supabase/migrations/001_init.sql` and document all columns of `pcr_results`, `batch_buyers`, and `customer_cycles`
  - [ ] Confirm `pcr_results.lab` is a text column (not a foreign key); replace the hardcoded "DOFR" string with this column
  - [ ] Confirm `batch_buyers` columns: `batch_id`, `customer_id`, `quantity`, `sold_at` (and any others)
  - [ ] Confirm `customer_cycles` columns relevant to D30: `batch_id`, `customer_id`, `d30`, `started_at`

- [ ] Update `getBatch()` in `lib/api/supabase.ts` to fetch real distribution and PCR data (AC: #1–#5)
  - [ ] Extend the Supabase query to JOIN `pcr_results` on `batch_id` — select all per-disease rows, not just a single PCR status flag
  - [ ] Extend the query (or add a second call) to fetch `batch_buyers` joined with `customers` for buyer name and `customer_cycles` for D30 — confirm the join path via FK before writing
  - [ ] Compute mean D30 server-side from the fetched `customer_cycles` rows; do not compute in the component
  - [ ] Compute sold PL count as `SUM(batch_buyers.quantity)` for the batch
  - [ ] Compute buyer farm count as `COUNT(DISTINCT batch_buyers.customer_id)`
  - [ ] Return a typed `BatchDetail` shape from `lib/types.ts`; extend the `Batch` type or add a `BatchDetail` supertype — no `any`

- [ ] Update mock `getBatch()` in `lib/mock/api.ts` to return the extended `BatchDetail` shape (AC: #1–#5)
  - [ ] Seed `pcr_results` mock rows for at least one batch with varied disease results and a realistic `lab` value (e.g., "กรมประมง")
  - [ ] Seed `batch_buyers` mock rows linking to existing mock customers
  - [ ] Seed `customer_cycles` mock rows with `d30` values spanning the full range (40%–95%) to exercise the histogram

- [ ] Replace hardcoded PCR section in `app/[locale]/(dashboard)/batches/[id]/page.tsx` (AC: #3)
  - [ ] Map over `batchDetail.pcrResults` array; render one row per disease
  - [ ] Display `pcr_results.lab` (Thai text from DB) in place of any hardcoded lab string
  - [ ] Render pass/fail using the existing PCR chip component; fall back to "รอผล" chip when result is null
  - [ ] If `pcrResults` array is empty, render a clear empty-state message

- [ ] Replace hardcoded buyers table in `app/[locale]/(dashboard)/batches/[id]/page.tsx` (AC: #4)
  - [ ] Map over `batchDetail.distributions` array; render one `<tr>` per distribution row
  - [ ] Show: farm name (from joined customer), quantity formatted with Thai locale number formatting, sold date in `DD MMM YYYY` (Buddhist Era year for Thai locale), D30 from linked cycle or "—" if absent
  - [ ] If `distributions` array is empty, render a clear empty-state message ("ยังไม่มีข้อมูลการจำหน่าย")

- [ ] Implement D30 distribution histogram (AC: #5)
  - [ ] Collect all `d30` values from `batchDetail.distributions` (via linked `customer_cycles`)
  - [ ] Bin into 10 equal-width buckets from 0–100% (each bucket = 10 percentage points)
  - [ ] Render using the existing chart primitive in `components/aw/` — check what is already available before adding a new chart library
  - [ ] If fewer than 3 values: render `<p className="...">ข้อมูลไม่เพียงพอสำหรับการแสดงกราฟ</p>` instead of the chart
  - [ ] The histogram must NOT throw or cause the page to error-boundary when data is absent

- [ ] Add 404 handling for invalid batch IDs (AC: #6)
  - [ ] In the server component, if `getBatch()` returns null, call Next.js `notFound()` to render the existing `not-found.tsx`
  - [ ] Do not render an empty page; always either show data or 404

- [ ] (Epic K) Implement the Farm Claims panel + publish toggle + warning button (AC: #7, #8, #9)
  - [ ] Extend `getBatch()` in `lib/api/supabase.ts` to also select `batch_claims` rows joined to the batch, plus the latest 5 `crm_event_log` rows scoped by `(batch_id, event_type='batch_warning')`. Return them on the `BatchDetail` shape as `claims: BatchClaim[]` and `warnings: CrmEventLogRow[]`.
  - [ ] Mock parity: seed 0/3/12 `batch_claims` rows across mock batches to exercise the empty state, normal density, and overflow truncation (panel pages to 10 with a "ดูเพิ่มเติม" link)
  - [ ] Page component reads the new fields and renders the panel; hide when `published_at IS NULL` per AC #7
  - [ ] Publish toggle calls a server action `togglePublishBatch(batchId, publish: boolean)` at `app/[locale]/(dashboard)/batches/actions.ts`; RBAC `can(role, 'batch:write')`; snapshots contact fields on publish
  - [ ] "แจ้งเตือนล็อตนี้" button opens the modal introduced in K4 — no logic in C3, just the button
  - [ ] Defence-in-depth: NEVER render a farmer's `line_user_id` directly on this page (only the display name + 8-char hashed pond id) — auditor role must not see anything personally identifying

- [ ] Write Vitest tests at `tests/batches/detail.test.ts` (AC: #2, #3, #4, #5)
  - [ ] Mean D30 computed correctly for a batch with mixed-D30 buyers (e.g., [60, 80, 90] → mean = 76.67%)
  - [ ] Mean D30 is `null` when no `customer_cycles` rows exist
  - [ ] PCR table renders 4 rows when 4 `pcr_results` rows exist; renders empty state when 0 rows
  - [ ] Lab name from `pcr_results.lab` is rendered; "DOFR" literal string does not appear anywhere in component output
  - [ ] Histogram renders empty state when fewer than 3 D30 values are provided
  - [ ] Histogram renders chart when 3 or more values provided; bucket counts sum to total input count

## Dev Notes

### Architecture Constraints
- All API calls via `lib/api/index.ts` facade; never call Supabase directly from a page component
- `'use client'` opt-in only; the histogram and any interactive chart controls need client marking, but the page layout and data fetch are server components
- No `any` in app code; extend `lib/types.ts` with a `BatchDetail` type that includes `pcrResults: PcrResult[]` and `distributions: BatchDistribution[]`
- `pcr_results` is the correct table name — `batch_pcr_tests` does NOT exist (drift resolved in MATRIX.md 2026-05-02)
- This page is blocked on C1.i: the page can be implemented and rendered in mock mode, but live data requires real `batches` + `pcr_results` rows from C1

### Files to Touch
- `app/[locale]/(dashboard)/batches/[id]/page.tsx` — replace hardcoded PCR section and buyers table; add histogram; add 404 guard
- `lib/api/supabase.ts` — extend `getBatch()` with distributions + pcr_results joins
- `lib/api/index.ts` — expose updated `getBatch()` returning `BatchDetail`
- `lib/mock/api.ts` — extend mock `getBatch()` with seeded distributions + pcr results
- `lib/types.ts` — add `BatchDetail`, `PcrResult`, `BatchDistribution` types
- `tests/batches/detail.test.ts` — new test file

### API Facade Methods
- `getBatch(hatcheryId: string, batchId: string): Promise<BatchDetail | null>` — extend existing; returns null on not-found
- No new write-path methods in this story

### RBAC
- `can(role, 'batch:read')` — gates access to the detail page; all roles with batch:read may view the page
- Auditor role: same data access as manager for batch detail (PCR and distribution counts are not commercial fields); `unit_price` from `batch_buyers` should be excluded for auditors consistent with C2

### RLS Tables
- `batches` — migration 001; `hatchery_id` RLS
- `pcr_results` — migration 001; RLS scoped by `hatchery_id` via `batches` FK — confirm the policy chain
- `batch_buyers` — confirm which migration introduced this table; verify `hatchery_id` RLS or FK-inherited scoping
- `customer_cycles` — confirm which migration introduced this table; verify RLS

> **CRITICAL:** `pcr_results` is the correct table. `batch_pcr_tests` does NOT exist. Read migration 001 to confirm column names before writing any query. Do not rely on MATRIX.md task descriptions that still reference the old name — the drift flag in MATRIX.md §C3 was not fully corrected in the task text (`batch_pcr_tests` appears in the C3.i task row — ignore it, use `pcr_results`).

### i18n Keys
Add to both `messages/th.json` and `messages/en.json`:
- `batches.detail.pcr.lab` — "ห้องปฏิบัติการ" / "Laboratory"
- `batches.detail.pcr.result` — "ผลตรวจ" / "Result"
- `batches.detail.pcr.date` — "วันที่ตรวจ" / "Test Date"
- `batches.detail.pcr.empty` — "ยังไม่มีผลตรวจ PCR" / "No PCR results yet"
- `batches.detail.buyers.empty` — "ยังไม่มีข้อมูลการจำหน่าย" / "No distribution data yet"
- `batches.detail.buyers.farm` — "ฟาร์ม" / "Farm"
- `batches.detail.buyers.quantity` — "จำนวน PL" / "PL Quantity"
- `batches.detail.buyers.soldAt` — "วันที่จำหน่าย" / "Sold Date"
- `batches.detail.buyers.d30` — "D30" / "D30"
- `batches.detail.histogram.title` — "การกระจาย D30 ของผู้ซื้อ" / "Buyer D30 Distribution"
- `batches.detail.histogram.insufficient` — "ข้อมูลไม่เพียงพอสำหรับการแสดงกราฟ" / "Not enough data to display chart"
- `batches.detail.stats.produced` — "ผลิต" / "Produced"
- `batches.detail.stats.sold` — "จำหน่ายแล้ว" / "Sold"
- `batches.detail.stats.buyers` — "ผู้ซื้อ" / "Buyers"
- `batches.detail.stats.meanD30` — "D30 เฉลี่ย" / "Mean D30"

### Testing
```bash
pnpm vitest run tests/batches/detail.test.ts
pnpm typecheck && pnpm lint
```

### References
- [Source: docs/product-spec/03-user-stories.md#C3]
- [Source: docs/work-breakdown/MATRIX.md#C3]
- [Source: docs/bmad/stories/K1.batch-code-and-claims-schema.md — `batch_claims`, `crm_event_log`]
- [Source: docs/bmad/stories/K4.batch-warning-webhook-publisher.md — publish-warning modal]
- [Source: aquawise-line-bot/docs/bmad/stories/K1.batch-code-claim-flow.md — producer of `batch_claims` rows]
- [Source: aquawise-line-bot/docs/bmad/stories/K12.hatchery-late-warning-broadcast.md — receiver of warnings rendered in AC #9]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
