# Story E2: Auto-Create Alerts from Farm D30 Dips

Status: draft

## Story

As a Manager,
I want to receive an alert when farms report D30 below target on a batch I produced,
so that I can investigate before the pattern spreads.

## Acceptance Criteria

1. A cron/trigger watches `farm_cycle_metrics.d30` rows posted from the AquaWise farm app
2. When ≥2 farms with the same source `batch_id` report D30 < 70%, generate an `alerts` row with `severity = 'medium'` and action text "ตรวจสอบล็อต"
3. When ≥3 farms or any farm reports D30 < 60% on the same `batch_id`, severity escalates to `'high'`
4. Notification delivered per the hatchery's Settings → Notifications toggles
5. De-duplication via partial unique index on `(batch_id, alert_kind, week)` — no duplicate alert for the same dip within the same week

## Tasks / Subtasks

- [ ] Task 1 — Cross-service coordination (BLOCKER — must complete before any code)
  - [ ] Confirm the farm-side `farm_cycle_metrics` table schema with the farm-app team: column names (`batch_id`, `d30`, `farm_id`, recorded-at), write cadence, RLS scope, and whether the hatchery Supabase project and the farm Supabase project are the same instance
  - [ ] If they are separate Supabase projects, agree on a cross-project access plan (foreign data wrapper, event webhook, or shared project merge) before sprint commit
  - [ ] Document the agreed schema and access strategy in `docs/work-breakdown/` before writing any migration
- [ ] Task 2 — Implement trigger or scheduled function (AC: #1, #2, #3)
  - [ ] Write migration `00X_d30_trigger.sql`: Postgres trigger (or pg_cron scheduled function) on `farm_cycle_metrics` that:
    - Groups rows by `batch_id` for the current ISO week
    - Counts farms with D30 < 70% and checks for any D30 < 60%
    - On threshold breach, `INSERT ... ON CONFLICT DO NOTHING` into `alerts` using the partial unique on `(batch_id, alert_kind, week)`
    - Sets `severity` to `'medium'` (≥2 farms, D30 < 70%) or `'high'` (≥3 farms or D30 < 60%)
    - Sets `action` to `'ตรวจสอบล็อต'`
  - [ ] Confirm partial unique index `(batch_id, alert_kind, week)` exists (check existing migrations); add it in this migration if missing
  - [ ] Verify the trigger fires for the right hatchery only — the `batches.hatchery_id` join must scope inserts correctly so cross-hatchery batch data never leaks
- [ ] Task 3 — Notification delivery (AC: #4)
  - [ ] After alert creation, trigger respects `notification_settings` for the hatchery — read `notification_settings.disease` toggle before enqueueing any `line_outbound_events` row
  - [ ] High-severity alerts bypass quiet hours (per H4 spec), but H4 is not yet implemented — add a comment in the trigger body noting this future dependency
- [ ] Task 4 — Write Vitest coverage (AC: #2, #3, #5)
  - [ ] `tests/alerts/auto.test.ts`:
    - 1 farm dip below 70% → no alert created
    - 2 farms dip below 70% → alert created with severity `medium`
    - 3 farms dip below 70% → alert severity escalates to `high`
    - Any farm dip below 60% → alert severity `high` regardless of farm count
    - Same dip event in the same week → no duplicate alert (idempotency)
- [ ] Task 5 — Live verification (AC: #1–#5)
  - [ ] On staging: simulate 2-farm D30 dip against a shared `batch_id`; confirm alert row appears in `alerts` within trigger latency
  - [ ] Confirm no duplicate row is created when the same farms dip again in the same week
  - [ ] Confirm alert surfaces correctly in the E1 UI at `/th/alerts`

## Dev Notes

> **BLOCKED — Do not start implementation until Task 1 (cross-service coordination) is resolved.**
>
> E2 requires `farm_cycle_metrics` from the farm-side AquaWise product (separate codebase). That table does not exist in this repo's Supabase project. Proceeding without confirming the schema, write cadence, and access boundary is a sprint risk — align with the farm-app team before committing to any migration.

### Architecture Constraints
- All API calls via `lib/api/index.ts` facade — never Supabase directly from pages
- The alert-creation path is server-side only (Postgres trigger or pg_cron function) — no client code is needed for this story
- No `any` in app code; adapters follow existing `rowToAlert` patterns in `lib/api/supabase.ts`
- The existing `listAlerts()` facade (E1) already surfaces the created alerts — no new read path needed

### Files to Touch

| File | Role |
|------|------|
| `supabase/migrations/00X_d30_trigger.sql` | New migration: trigger + partial unique index |
| `tests/alerts/auto.test.ts` | New Vitest unit tests for threshold logic |
| `docs/work-breakdown/MATRIX.md` | Update E2.i status once cross-service decision is documented |

### API Facade Methods
No new facade methods required — `listAlerts()` (E1) already reads the created alerts. The trigger writes directly to `alerts` using a service-role context inside the Postgres function.

### RBAC
- Alert creation happens inside a Postgres function running with elevated privileges — no `can()` check needed in the trigger itself
- The Manager persona (P2) is the notification target; delivery respects `notification_settings.disease` toggle
- `alert:close` (E3) and read access are unchanged

### RLS Tables
| Table | Operation | Migration |
|-------|-----------|-----------|
| `alerts` | INSERT (via Postgres trigger, service-role context) | `002_rls.sql` + new `00X_d30_trigger.sql` |
| `farm_cycle_metrics` | SELECT (read from farm-side; cross-project access TBD) | farm-side schema (external) |
| `line_outbound_events` | INSERT (notification enqueue) | `006_line_integration.sql` |

The partial unique on `(batch_id, alert_kind, week)` is the de-dup mechanism — confirm it exists or add it in the new migration.

### Threshold Logic Summary
```
week = date_trunc('week', now())

dip_count  = COUNT(*) WHERE batch_id = $batch_id AND d30 < 0.70 AND week = $week
severe_dip = EXISTS(   WHERE batch_id = $batch_id AND d30 < 0.60 AND week = $week)

IF dip_count >= 3 OR severe_dip → severity = 'high'
ELSE IF dip_count >= 2          → severity = 'medium'
ELSE                            → no alert
```

### i18n Keys
No new i18n keys expected for this story — alert creation is server-side and uses hardcoded Thai action text (`'ตรวจสอบล็อต'`). If the action text is later moved to a translatable field, add under `alerts.actions` namespace in both `messages/th.json` and `messages/en.json`.

### Testing
```bash
# Run the auto-alert unit tests (once Task 4 is complete)
pnpm vitest run tests/alerts/auto.test.ts

# Typecheck and lint (must be clean before marking .v done)
pnpm typecheck
pnpm lint
```

### References
- [Source: docs/product-spec/03-user-stories.md#E2]
- [Source: docs/work-breakdown/MATRIX.md#E2]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
