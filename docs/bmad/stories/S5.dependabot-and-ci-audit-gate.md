# Story S5: Dependabot + CI `pnpm audit` Gate

Status: ready-for-dev

Severity: P1 — pre-launch hardening

## Story

As an Owner,
I want automated weekly PRs for dependency upgrades and a CI step that fails builds with high-severity advisories,
so that a `next` (or any other) vulnerability does not sit unpatched while we ship features.

## Acceptance Criteria

1. `.github/dependabot.yml` exists, scans `npm` ecosystem weekly, opens up to 10 PRs at a time, applies a `dependencies` label
2. `.github/workflows/ci.yml` includes a step `pnpm audit --audit-level=high` that **fails the build** on any unaddressed high-severity finding
3. The same workflow also runs `pnpm typecheck`, `pnpm lint`, and `pnpm test`
4. A Dependabot PR landing on a feature branch triggers CI automatically
5. README or `docs/bmad/code-design.md` references the policy for handling Dependabot PRs (review and merge weekly; high-severity must be merged or explicitly waived within 48 hours)

## Tasks / Subtasks

- [ ] Task 1 — Dependabot config (AC: #1)
  - [ ] Create `.github/dependabot.yml`:
    ```yaml
    version: 2
    updates:
      - package-ecosystem: "npm"
        directory: "/"
        schedule:
          interval: "weekly"
          day: "monday"
        open-pull-requests-limit: 10
        labels:
          - "dependencies"
        groups:
          dev-dependencies:
            dependency-type: "development"
          radix-ui:
            patterns: ["@radix-ui/*"]
          next-stack:
            patterns: ["next", "next-intl", "react", "react-dom"]
    ```
  - [ ] Grouping reduces PR noise — three groups: dev-deps, Radix UI, Next.js core. Other deps stay one-per-PR.
- [ ] Task 2 — CI workflow (AC: #2, #3, #4)
  - [ ] Create or update `.github/workflows/ci.yml`:
    ```yaml
    name: CI
    on:
      pull_request:
      push: { branches: [main] }
    jobs:
      gates:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: pnpm/action-setup@v4
            with: { version: 10 }
          - uses: actions/setup-node@v4
            with: { node-version: 20, cache: pnpm }
          - run: pnpm install --frozen-lockfile
          - run: pnpm audit --audit-level=high
          - run: pnpm typecheck
          - run: pnpm lint
          - run: pnpm test
    ```
- [ ] Task 3 — Document policy (AC: #5)
  - [ ] Add to `docs/bmad/code-design.md` §13 (security checklist): "Dependabot PRs merge weekly; high-severity advisories ship within 48 hours or a §19 entry explains the deferral."
  - [ ] Add to `security.md` §10: link to this story as the ongoing mechanism
- [ ] Task 4 — Verify in PR
  - [ ] After merging this story, the next push triggers CI
  - [ ] Confirm `pnpm audit` step appears in the Actions tab
  - [ ] Confirm a high-severity advisory (manually inject if needed — pin an old `next` for one commit, then revert) fails CI

## Dev Notes

### Architecture Constraints
- pnpm version is `10.0.0` — use `pnpm/action-setup@v4 with: { version: 10 }`, not the default
- Node version 20+ per `package.json` `engines`
- `pnpm install --frozen-lockfile` enforces lockfile consistency

### Files to Touch
| File | Change |
|------|--------|
| `.github/dependabot.yml` | NEW |
| `.github/workflows/ci.yml` | NEW or extend existing |
| `docs/bmad/code-design.md` | §13 checklist addition |
| `docs/bmad/security.md` | §10 cross-reference to this story |

### Edge cases
- If a transitive dependency has a high-severity advisory but no upstream patch exists yet, document in `code-design.md` §19 ("Deferred advisory: X, reason: no patch released; mitigation: Y") and add to `pnpm audit --audit-level=high --ignore=<advisory-id>` so CI doesn't block forever. Re-evaluate weekly.
- Dependabot may open PRs that fail tests on minor breaking changes. The dev team treats those like any other failing PR — fix or close, don't merge red.

### RBAC / RLS / i18n
- None — pure CI/CD config.

### Source
- `docs/bmad/security.md` §10 (Dependency / supply-chain risk)
- GitHub Dependabot config syntax: https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

### Verification
```bash
# After merge:
# Wait for Monday → Dependabot opens a batch of PRs
# Check Actions tab → CI runs on each
# Verify `pnpm audit` step is visible and the build fails if a high-severity issue appears
```
