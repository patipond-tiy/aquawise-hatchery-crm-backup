# Story B1: See All Customers at a Glance

Status: ready-for-dev

## Story

As a Rep,
I want to browse my customer list, search by farm or owner name, and filter by lifecycle status,
so that I can quickly find the nursery operator I need.

## Acceptance Criteria

1. `/customers` lists all customers belonging only to this hatchery (RLS-enforced — no cross-tenant leakage)
2. Search filters by `farm`, `farmEn`, and `name` fields (case-insensitive, client-side debounce)
3. Tabs: All / Active (`cycleDay != null`) / Restock (`restockIn ≤ 14`) / Concern
4. Each card shows farm name, owner, status chips, batches count, LTV, last D30
5. Card click navigates to customer detail; 💬 icon opens the `sendLine` modal
6. Customers without a `customer_cycles` row appear in the list (not silently dropped)

## Tasks / Subtasks

- [ ] Task 1 — RLS cross-tenant regression test (AC: #1, #6)
  - [ ] Add test in `tests/api/list-customers.test.ts` asserting that a Supabase client authenticated as `hatchery_B_member` receives zero rows from `hatchery_A`'s customers (the left-join fix from B1.i, commit `ddf6ea3`, is already in place; this test locks it against regression)
  - [ ] Confirm `lib/api/supabase.ts` `listCustomers` select string does NOT contain `!inner` on `customer_cycles`

- [ ] Task 2 — Live verification against Supabase (AC: #1–#6)
  - [ ] Set `USE_MOCK=false` + `NEXT_PUBLIC_USE_MOCK=false` in `.env.local` with a real Supabase project
  - [ ] Sign in as a rep-role user; confirm `/th/customers` renders the RLS-scoped list
  - [ ] Verify search filters all three fields case-insensitively
  - [ ] Verify tab filters (Active / Restock / Concern) bucket correctly against live data
  - [ ] Verify a net-new customer with no cycle row appears in the All tab with null cycle fields
  - [ ] Confirm card LTV and last D30 come from real DB values, not mock seed

## Dev Notes

### Domain Framing — Critical
"Customers" in Epic B are the **~20–40 nursery operators** (`โรงอนุบาล`) the hatchery sells post-larvae (PL) to. They are NOT end farms. The customer list is a **relationship dashboard**, not a sales-pipeline CRM. The "farm" vocabulary in the schema is scaffolding from an earlier iteration; the exact UX framing is a hypothesis pending validation with P'Bunjong in 2027. Do not over-invest in pipeline-CRM metaphors.

### Implementation Status
- **B1.i is ✅** — left-join fix landed in commit `ddf6ea3`. `lib/api/supabase.ts:104–122` now left-joins `customer_cycles` so customers without a cycle row are returned.
- **B1.t is 🟡** — unit regression test exists (`tests/api/list-customers.test.ts`); the **cross-tenant RLS test is missing** (deferred to P0.3 in MATRIX).
- **B1.v is ❌** — live Supabase verification not yet done.
- This story's remaining work is **RLS test + live verification only**. No UI changes required.

### Architecture Constraints
- All API calls via `lib/api/index.ts` facade — never call Supabase directly from a page
- `'use client'` opt-in only — `app/[locale]/(dashboard)/customers/page.tsx` should remain a server component where possible
- No `any` in app code

### Files to Touch
- `tests/api/list-customers.test.ts` — add cross-tenant RLS assertion
- `lib/api/supabase.ts` (read-only verification) — confirm no `!inner` on line ~111

### API Facade Methods
- `listCustomers(hatcheryId: string): Promise<Customer[]>` — already implemented in `lib/api/supabase.ts`

### RBAC
- `can(role, 'customer:read')` — passes for `owner`, `counter_staff`, `lab_tech`, `auditor`
- Page access: all authenticated roles; no `customer:write` needed for list view

### RLS Tables
- `customers` — RLS scoped to `hatchery_members.hatchery_id` of calling user (migration `001_init.sql`)
- `customer_cycles` — left-joined; verify RLS permits read for all four roles

### i18n Keys
No new keys required — list page and card labels already exist. If any label is missing, add to both files:

| Key | `messages/th.json` | `messages/en.json` |
|-----|-------------------|-------------------|
| (none new) | — | — |

### Testing
```bash
pnpm vitest run tests/api/list-customers.test.ts
pnpm typecheck && pnpm lint
```

### References
- [Source: docs/product-spec/03-user-stories.md#B1]
- [Source: docs/work-breakdown/MATRIX.md#B1]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
